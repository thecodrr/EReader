<Page
    x:Class="EReader.Views.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EReader.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <RelativePanel x:Name="Header" Grid.Row="0" Margin="20" VerticalAlignment="Center">
            <TextBlock x:Name="Title" Text="Title" FontSize="36" HorizontalAlignment="Left"/>
        </RelativePanel>
        <Frame x:Name="ContentFrame" Grid.Row="1" Margin="8"/>
    </Grid>
</Page>
